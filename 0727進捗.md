# 0727進捗メモ

Last updated: 2026-07-19 23:30 JST

## 目的

2026-07-27頃の進捗報告に向けて、直近約10日間の実装・評価結果・考察・残課題を整理する。
特に、V2V単体、V2N2V単体、Hybrid、Hybrid+RMRを比較し、RMRがV2N2V経路を有効化する要因を説明できるようにする。

## 結論の要約

- Hybrid baselineはV2V単体よりORRが改善するが、MEC側のAoI違反と更新失敗が大きく、改善幅は限定的である。
- Hybrid+RMRではORRが69.7%から98.6%へ大きく改善した。
- 改善要因は平均CBRの低下ではなく、V2N2V経路のCPM payload削減により、MEC DL busy、AoI、更新失敗が改善したこと。
- V2V単体+RMRではORRが改善せず、V2VだけにRMRを入れれば十分という説明は支持されなかった。
- V2N2V単体+MEC fanout RMRは途中ログでは大きく削除してもMEC tail delayとAoI違反が残り、Hybridによる経路分散の必要性が強い。
- 次はロングテールRMRとstrict TTL感度分析で、改善がTTL設定や固定削除数に依存していないかを確認する。

## 使用した主な結果パス

| 手法 | 結果パス |
|---|---|
| V2V only / no-control | `results/bg500_base3_seed30_100s_retry/no-control/summary.csv` |
| V2V only + RMR | `results/bg500_v2v_only_rmr_fixed_seed30_100s/no-control/summary.csv` |
| V2N2V only | `results/bg500_v2n2v_only_fixed_seed30_100s_retry/v2n2v-only/summary.csv` |
| Hybrid baseline | `results/bg500_hybrid_baseline_delooz_seed30_100s/v2n2v-adaptive-probability/summary.csv` |
| Hybrid + RMR | `results/bg500_hybrid_rmr_delooz_seed30_100s/v2n2v-adaptive-probability/summary.csv` |

## 主要結果

| 指標 | V2V only | V2N2V only | Hybrid baseline | Hybrid + RMR |
|---|---:|---:|---:|---:|
| ORR [%] | 61.3 | 13.9 | 69.7 | 98.6 |
| High priority ORR [%] | 62.3 | 24.5 | 70.5 | 98.8 |
| Low priority ORR [%] | 59.6 | 0.0 | 68.5 | 98.4 |
| Packet loss rate [%] | 63.8 | 100.0 | 75.2 | 31.5 |
| V2V radio loss rate [%] | - | - | 63.8 | 55.7 |
| MEC update failure rate [%] | - | 100.0 | 79.5 | 14.4 |
| MEC valid update success rate [%] | - | 0.0 | 20.5 | 85.6 |
| MEC AoI violation >200ms [%] | - | 99.9 | 78.1 | 22.3 |
| MEC AoI p99 [ms] | 0 | 84396 | 25579 | 249 |
| MEC DL busy p99 [%] | - | - | 100.0 | 91.2 |
| Channel busy ratio [%] | 80.2 | 68.4 | 80.2 | 80.2 |

## 2026-07-19 00:20 JST 直近で追加確認できた結果

### V2V単体+RMRは、V2V単体よりORRが低下した

`results/bg500_v2v_only_rmr_fixed_seed30_100s/no-control/summary.csv` が完走した。
同じBG500、seed30、100s条件で比較すると、V2V単体にRMRを適用してもORRは改善せず、むしろ低下した。

| 指標 | V2V only | V2V only + RMR |
|---|---:|---:|
| ORR [%] | 61.3 | 54.8 |
| High priority ORR [%] | 62.3 | 54.3 |
| Low priority ORR [%] | 59.6 | 55.0 |
| Channel busy ratio [%] | 80.2 | 80.2 |
| Packet loss rate [%] | 63.8 | 55.7 |
| V2V update failure rate [%] | - | 80.0 |
| Receiver RL mean | - | 0.495 |
| Receiver RV mean | - | 0.0936 |

読み取り:

- V2V単体+RMRではpacket/radio lossは下がったが、ORRは61.3%から54.8%へ低下した。
- V2V単体では、RMRによるCPM削減が受信機会の減少に直結しやすい。
- したがって、「V2V単体にRMRを入れれば十分」という説明は、この条件では支持されない。
- Hybrid+RMRが98.6%まで改善した主因は、V2V側のCBR低下ではなく、MEC/V2N2V経路のAoIと有効更新成功率が改善したことだと説明できる。

### V2N2V単体+MEC fanout RMRは途中ログで厳しい傾向

V2N2V単体に軽量MEC fanout RMRを適用した途中ログでは、削除数を増やしてもMEC downlink tail delayとAoI違反が残った。
したがって、V2N2V単体を強く削ればHybrid+RMRに近づく、とは現時点では言いにくい。

| 条件 | 時刻 | ORR | MEC RMR deleted | MEC AoI>200ms | MEC failure | MEC delay p99 |
|---|---:|---:|---:|---:|---:|---:|
| RMR 10/20/40 | 31s | 17.3707 | 2,277,539 | 99.6666 | 100 | 39,520 ms |
| RMR 20/40/80 | 10s | 18.0676 | 1,235,422 | 98.9239 | 100 | 7,196 ms |

読み取り:

- V2N2V単体では全車両更新がMEC fanoutへ集中し、削除してもキュー遅延の尾部が残る。
- 固定的に削除数を増やすだけでは、序盤の削りすぎと高負荷時の削り不足を同時に避けにくい。
- そのため、MEC DL busy ratioに応じて削除数を `10/20/40/80/160` に変えるロングテールRMRを次に評価する。
- V2V単体ではロングテールRMRの主効果が出にくいため、優先度を下げる。

## スライド用グラフ

### 全体比較

![Main comparison](results/slide_progress_figures/slide_01_main_comparison.png)

### ORR・CBR・MECボトルネック

![ORR CBR MEC bottleneck](results/slide_progress_figures/slide_06_orr_cbr_mec_bottleneck.png)

### Hybrid baseline と Hybrid+RMR のMEC側ボトルネック

![MEC side bottleneck](results/slide_progress_figures/slide_08_mec_side_bottleneck_rmr.png)

### Hybrid baseline と Hybrid+RMR の要約

![Compact hybrid RMR metrics](results/slide_progress_figures/slide_05_compact_hybrid_rmr_key_metrics.png)

### ORR時系列

![ORR timeseries](results/slide_progress_figures/slide_02_orr_timeseries.png)

### RL/RV時系列

![RL RV interpretation](results/slide_progress_figures/slide_04_rl_rv_interpretation.png)

## 結果の解釈

### 1. V2N2V経路を使うならRMRがほぼ必須

V2N2V onlyではMEC update failure rateが100%、MEC AoI violation >200msが99.9%となり、MEC経由更新がほぼ有効認識に使えていない。
Hybrid baselineでもMEC update failure rateは79.5%、MEC AoI violation >200msは78.1%であり、V2N2V経路は強いAoI制約を受けている。

一方、Hybrid+RMRではMEC update failure rateが14.4%、MEC AoI violation >200msが22.3%まで低下し、ORRは98.6%まで改善した。
したがって、現条件ではV2N2V経路はRMRにより初めて有効な補完経路として機能したと説明できる。

### 2. Hybrid baselineがV2VとV2N2Vの単純和にならない理由

ORRは同じ物体を複数経路で受信しても1回の認識として数えるため、V2V ORRとV2N2V ORRは足し算できない。
また、両経路の認識対象には重複があり、V2N2V経路ではAoI制約により多くの更新が無効化される。
そのため、Hybrid baselineのORRはV2V単体とV2N2V単体の単純和にはならない。

### 3. 改善要因は平均CBR低下ではなくMEC側の鮮度改善

平均CBRはHybrid baselineとHybrid+RMRでほぼ同じである。

| 指標 | Hybrid baseline | Hybrid + RMR |
|---|---:|---:|
| Channel busy ratio [%] | 80.2389 | 80.2023 |
| MEC DL busy p90 [%] | 100.0 | 84.9 |
| MEC DL busy p99 [%] | 100.0 | 91.2 |
| MEC AoI p99 [ms] | 25579 | 249 |
| MEC AoI >200ms [%] | 78.1 | 22.3 |
| NR CPM payload total [MB] | 12.92 | 4.47 |

つまりRMRは平均CBRを大きく下げたのではなく、V2N2V経路に流れるCPM payloadを削減し、MEC DL busyとAoIを改善した。
このため、MEC経由の有効更新成功率が上がり、ORRが改善したと解釈できる。

### 4. V2V radio lossが50%台に残ることの解釈

Hybrid+RMRでもV2V radio loss rateは55.7%であり、Sidelink単体では依然として損失が大きい。
これは山崎氏の研究でパケットロス率が50%台だった傾向と整合的に見える。

ただし、今回のORRは物体認識単位であり、パケット単位の損失率とは直接一致しない。
V2Vで失われた情報をV2N2V経路が補完することで、V2V radio lossが50%台でもORR 98.6%を達成できたと説明する。

## 懸念点: V2V単体+RMRとの比較が必要

V2V only + RMRの100秒結果により、前回教授に指摘された「V2N2Vを使わず、V2V単体にRMRを入れればよいのでは」という疑問には一定程度答えられるようになった。
今回の条件では、V2V単体にRMRを入れてもORRは改善せず、V2V onlyの61.3%からV2V only + RMRの54.8%へ低下した。

ただし、山崎氏の研究でRMRがV2Vに対して使われていたことを踏まえると、本研究では以下の比較として整理する。

| 比較 | 意味 |
|---|---|
| V2V only | 現在のSidelink単体基準 |
| V2V only + RMR | 山崎氏研究に近い基準。必須比較 |
| Hybrid baseline | V2N2V追加のみの効果 |
| Hybrid + RMR | 本研究の主張対象 |

この比較から、現時点では次のように説明できる。

- V2V+RMRではpacket/radio lossは下がるが、認識に必要な冗長性も削り、ORRが低下する。
- Hybrid+RMRではV2N2V経路のAoI悪化をRMRが緩和し、V2Vで不足した情報をMEC経路が補完できる。
- したがって、RMRの効果はV2V単体よりも、V2N2Vを含むHybridで顕著に現れる。

### V2V+RMRの100秒結果

`results/bg500_v2v_only_rmr_fixed_seed30_100s/no-control/summary.csv` の100秒結果では、V2V+RMRはRMRなしV2VよりORRが低い。

| 指標 | V2V no RMR | V2V + RMR |
|---|---:|---:|
| ORR [%] | 61.3192 | 54.8371 |
| High priority ORR [%] | 62.3321 | 54.3017 |
| Low priority ORR [%] | 59.5805 | 54.9982 |
| CBR [%] | 80.2389 | 80.2023 |
| packet/radio loss [%] | 63.8187 | 55.7377 |
| Receiver RL mean | - | 0.495277 |
| Receiver RV mean | - | 0.093639 |

RMRによりpacket/radio lossは約8.1ポイント下がったが、ORRは約6.5ポイント低下した。
したがって、V2V単体ではRMRによる削減が認識機会の減少として働きやすい。

### RMRがV2V単体よりV2N2V/Hybridで効きやすい理由

現時点ではV2N2V単体+RMRを直接評価していないため断定は避ける。
ただし、これまでの結果からは、RMRはV2V単体よりもV2N2Vを含むHybridで改善効果が大きい傾向がある。

理由は以下である。

1. V2V単体では、CPMから物体情報を削除すると、その情報の受信機会が直接減る。
   V2Vでは無線ロスが50%以上残るため、本来ロス耐性として必要な冗長情報まで削るとORRが低下しやすい。

2. V2N2V/MEC経路では、主なボトルネックが無線ロスではなくMEC下りの混雑、キュー滞留、AoI悪化である。
   Hybrid baselineではMEC update failure rateが79.5%、MEC AoI violation >200msが78.1%、MEC AoI p99が25579msであり、情報が古すぎて有効認識に使えていない。

3. RMRによりMEC経路のCPM payloadが削減されると、MEC DL busyとAoIが改善する。
   Hybrid+RMRではMEC update failure rateが14.4%、MEC AoI violation >200msが22.3%、MEC AoI p99が249msまで改善した。

4. MEC経路では多数車両から情報が集約されるため、同一物体に対する冗長情報が増えやすい。
   そのため、RMRで削除しても認識に必要な情報が他車両・他時刻・他経路から補完されやすい。

5. Hybridでは、V2Vが直接・低遅延の認識を担い、V2N2VがV2Vで落ちた情報や範囲外情報を補完する。
   RMRはこのV2N2V補完経路をAoI制約内で機能させる役割を持つ。

進捗報告での表現案:

> V2V単体ではRMRによる削除が受信機会の減少に直結しやすく、無線ロスに対する冗長性を削ってしまう可能性がある。一方、V2N2V経路ではMEC側に情報が集約されるため冗長情報が多く、主なボトルネックはMEC下りの混雑とAoI悪化である。そのため、RMRはV2N2V経路では不要・重複情報を削減してAoIを改善し、有効更新成功率を高める方向に働く。結果として、RMRの改善効果はV2V単体よりV2N2Vを含むHybridで顕著に現れた可能性がある。

今後の確認事項:

- V2N2V only + RMRを追加評価し、V2N2V経路単体でもRMRがAoI改善に効くかを確認する。
- 経路別RMRを導入し、V2V側は冗長性保護、V2N2V側はAoI改善を目的とする制御に分離する。

## Delooz RL/RV診断の結果

| 指標 | Hybrid baseline | Hybrid + RMR |
|---|---:|---:|
| Receiver RL mean | 1.3086 | 0.4953 |
| Receiver RL median | 0.2692 | 0.0658 |
| Receiver RL >= 1 [%] | 24.8 | 10.7 |
| Receiver RL >= 2 [%] | 15.9 | 4.2 |
| Delooz RV mean | 0.2302 | 0.0936 |

Hybrid+RMRはORRを大幅に改善したが、受信側の冗長性は低下している。
これはRMRが不要情報を削減してAoIを改善した一方で、パケットロス耐性としての冗長性を削っていることを示す。

今後は、単に削除数を増やすのではなく、RL/RVを見ながら「削ってよい情報」と「残すべき冗長情報」を分けるRL-aware RMRが有望である。

## 直近約10日間で行ったこと

### 指標・集計の修正

- AoI制約付きORRを導入した。
- AoI閾値ごとのORRを200ms、300ms、400ms、500msで出せるようにした。
- 無線由来のpacket loss、AoI違反、TTL違反、never receivedを分離して集計するようにした。
- V2N2VだけでなくV2V側にも更新失敗・無線ロス・AoI関連指標を適用した。
- HybridでMEC転送数やDL lossが正しく集計されない問題を修正した。
- `mec_forwarded_packets`、`mec_dl_final_losses`、`mec_update_failure_rate`などの集計を確認した。

### 背景負荷・公平性の修正

- V2V経路とV2N2V経路に同じ背景負荷をかける方針にした。
- Hybridで「帯域を増やしただけ」と見られないよう、NR BGとMEC BGを同条件で設定した。
- BG500条件でV2V only、V2N2V only、Hybrid、Hybrid+RMRを比較できるようにした。

### 文献・仕様の整理

- AoI閾値は一律200msだけでなく、200-500msの感度分析として説明する方針にした。
- MEC転送範囲200mについて、現時点では評価条件として扱い、500mは余裕があれば感度分析する位置づけにした。
- 距離要因のロスよりも、今回の条件では帯域混雑・AoI違反が支配的であると整理した。
- DeloozのRL/RVを、RMR評価の診断指標として導入した。

### シミュレーション実行

- V2V only / no-control / BG500 / seed30 / 100sを取得した。
- V2N2V only / BG500 / seed30 / 100sを修正済み指標で取得した。
- Hybrid baseline / BG500 / seed30 / 100sを取得した。
- Hybrid+RMR / BG500 / seed30 / 100sを取得した。
- V2V only + RMR / BG500 / seed30 / 100sを取得し、V2V単体ではRMRがORRを改善しないことを確認した。
- 予測ありHybridも途中まで確認したが、初期実装では改善が見えず、RMRを優先した。
- RMR+Delooz診断の100秒評価を実行し、RL/RVログが出ることを確認した。

### 可視化

- V2V/V2N2V/Hybrid/Hybrid+RMRのORR比較グラフを作成した。
- Hybrid baselineとHybrid+RMRのMEC DL busy、AoI p99、AoI violation、CPM payload時系列を作成した。
- ORR、RL、RVの時系列比較グラフを作成した。

## 次にやるべきこと

## 途中ログ: V2N2V only + 軽量MEC RMR

更新日時: 2026-07-19 22:16 JST

対象:

- `results/bg500_v2n2v_only_light_mec_rmr_seed30_100s/v2n2v-only/observation.csv`
- V2N2V only / BG500 / seed30 / 100s設定
- `MEC_IDEAL_LINK=true`
- 軽量V2N2Vモデル上のMEC fanout RMR
- RMR削除数: `10/20/40`

途中時刻の観測値:

| time_s | ORR | mec_rmr_deleted_total | mec_aoi_violation_rate_200ms | mec_update_failure_rate | mec_capacity_delay_p99_ms |
|---:|---:|---:|---:|---:|---:|
| 31 | 17.3707 | 2,277,539 | 99.6666 | 100 | 39,520 |

補足:

- `mec_dl_busy_ratio=0.416855`、`mec_dl_offered_mbps=37.712`であり、観測時点の平均DL busyだけを見ると飽和には見えない。
- 一方で`mec_capacity_delay_p90_ms=39037`、`mec_capacity_delay_p99_ms=39520`であり、過去に発生した大きなMEC downlink fanoutキューが長時間残っている。
- `mec_rmr_candidates_total=3,806,650`、`mec_rmr_deleted_total=2,277,539`まで削除しても、AoI 200ms制約違反とMEC update failureはほぼ解消していない。
- この途中結果から、V2N2V単体では全車両がMEC経路へ集中し、RMR `10/20/40`だけではMEC downlink tail delayを抑えきれない可能性が高い。
- 100秒完走結果ではなく途中ログなので、最終評価値としては使わない。傾向確認および次実験の優先順位決定に用いる。

この結果を受け、単純な強RMR `20/40/80`よりも、MEC DL busy ratioに応じて削除数を変えるロングテールRMRを優先する。
目的は、低負荷時の過剰削除を避けつつ、高負荷時だけ大きくfanoutを絞ることで、V2N2V単体およびHybridのMEC tail delayを抑えられるかを確認することである。

## 2026-07-19 23:30 JST 追加した仕様・実験方針

### ロングテールMEC fanout RMR

MEC fanout RMRは、通常のCPM object RMRとは異なり、MEC downlinkの転送先を削る制御である。
現在の実装では、低重要度の転送先を優先して削り、同じ重要度なら遠い転送先を優先して削る。

ロングテールRMRでは、MEC DL busy ratioに応じて削除数を段階的に変える。

| MEC DL busy ratio | 削除数 |
|---:|---:|
| `<= rmr-cbr-low` | 10 |
| `<= rmr-cbr-high` | 20 |
| `<= 0.85` | 40 |
| `<= 0.95` | 80 |
| `> 0.95` | 160 |

狙い:

- 低負荷時は削除数10に留め、不要な認識機会の喪失を避ける。
- 高負荷時だけ80/160まで増やし、MEC downlink queueの尾部遅延を抑える。
- 将来的な帯域予測は、経路選択よりもこの削除強度を事前調整する用途に接続する。

### strict TTL感度分析

現行TTLは、高重要度 `0.2s`、低重要度 `0.5s` である。
低重要度 `0.5s` は低リスク対象なら許容可能だが、標準で一意に決まった値ではない。
そのため、次の感度分析として高重要度 `0.1s`、低重要度 `0.2s` の厳しめ条件を追加する。

比較優先度:

1. `Hybrid + longtail RMR + current TTL`
2. `Hybrid + longtail RMR + strict TTL`
3. `V2N2V only + longtail RMR`

strict TTLでもHybrid+RMRが優位なら、ORR改善がTTL設定の甘さだけに依存していないと説明しやすい。

### 最優先

1. Hybrid + longtail RMR / current TTLを完走させる。
2. Hybrid + longtail RMR / strict TTLを完走させる。
3. V2N2V only + longtail RMRを確認し、単体V2N2Vの限界を示す。
4. Hybrid+RMR / seed違いを使い、seed30だけの偶然ではないか確認する。

### 次点

1. Hybrid+RMR+predictionを再設計する。
2. 予測は単純に経路選択を増やすのではなく、RMR削除強度を事前調整する用途にする。
3. 単一山場ではなく、時間変動BGによる周期的な帯域逼迫シナリオで予測の有効性を見せる。

### 発展

1. RL-aware RMRを検討する。
2. 受信側RL/RVを送信側が推定またはフィードバックし、RL<1では削除しない、RL>=2では削除しやすくする。
3. 車線変更を含むより現実的な道路環境で、予測やRL/RVの価値を検証する。

## 進捗報告での推奨ストーリー

1. まず、V2V only、V2N2V only、Hybrid baseline、Hybrid+RMRのORRを見せる。
2. Hybrid baselineはV2V単体より改善するが、MEC AoIと更新失敗が大きく、単純なV2N2V追加だけでは不十分と説明する。
3. RMRによりMEC側のCPM payloadが約65%削減され、MEC DL busy、AoI、更新失敗が改善したことを示す。
4. その結果、ORRが69.7%から98.6%へ改善したと説明する。
5. 最後に、次の課題としてV2V+RMR比較とRL-aware RMR、帯域予測によるRMR制御を示す。

## スライドでの注意点

- 平均CBRはHybrid baselineとHybrid+RMRでほぼ同じなので、RMRの改善要因として平均CBR低下を主張しない。
- 主張すべき改善要因はMEC側AoI、MEC update failure、CPM payload削減である。
- V2V radio lossが50%台に残る点は弱点ではなく、V2N2V補完の必要性を説明する材料にする。
- V2V+RMRは完走済みで、ORRはV2V単体より低下した。これはHybrid+RMRの必要性を説明する材料として使える。
- V2N2V only + MEC fanout RMRの途中ログでは、削除してもAoI違反とMEC failureが大きく残った。完走値ではないため、単体V2N2Vの限界を示す補助材料として扱う。
- TTLは現行値だけで断定せず、strict TTL感度分析を次に取る。
