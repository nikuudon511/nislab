ご要望に合わせて、元スクリプトの残存する不備のまとめと、Codexに入力するための最適化されたシステム構築プロンプトをMarkdown形式で作成しました。

これをそのまま開発の指示書として、あるいはCodexへの入力としてご活用ください。

---

# V2Xハイブリッド経路制御システム 実装修正依頼書

## 1. 元スクリプトにおける致命的な不備の抽出
提供された `V2VHybridCbr80211pNrv2x.cc` を論文仕様（山崎論文および最新レジュメ）と比較した結果、以下の5点が実装上の不備として特定されました。

| 不備項目 | 現状の実装内容 | 論文・仕様書が求める正解 |
| :--- | :--- | :--- |
| **制御の粒度** | `ActivateRoute` により、**車両単位**で無線機を完全に切り替えている。 | **オブジェクト（物標）単位**で、同一パケット内でV2VとV2Nへ振り分ける必要がある。 |
| **CBR予測ロジック** | 自車両が計測した過去のCBRの傾きから予測する**線形回帰モデル**。 | RSUが流入・流出交通量から算出する**流体の連続の式モデル**。 |
| **重要度判定スコア** | 単なる距離や標準の冗長性ルールを使用。 | 山崎論文独自の **$R$ スコア ($R = N / (L \cdot \min(l, v))$)** による定量評価。 |
| **二重送信の実装** | 1つの経路のみを選択。 | 逼迫状態において、高重要情報を **V2VとV2Nの両方から同時に送信** する。 |
| **実装スコープの混線** | NR-V2Xのコードが大量に混在し、計算負荷を圧迫。 | 今月末の提出に向け、**NR-V2XおよびDENMロジックを完全排除**しMECに集中。 |

---

## 2. Codex用 実装ゴール設定プロンプト

以下のブロックをCodexの指示入力としてコピー＆ペーストしてください。

```markdown
# Role
ns-3 (VaN3Twin) シミュレーションエンジニア。V2X通信およびITSスタックの専門家。

# Goal
提供されたベースコードを修正し、修士論文の核となる「交通流予測に基づくV2V/V2N適応型ハイブリッド経路制御」を厳密に実装すること。

# Inputs (Context)
- `resume_0604.md`: 提案手法（通常・警戒・逼迫の3状態）の設計図。
- `研究内容詳細.md`: 評価環境（RSU予測式、干渉ノード、重要度分類）の仕様。
- `master_thesis_SYamasaki`: 山崎論文の $R$ スコア計算定義。

# Strict Implementation Constraints (絶対防衛線)
1. **RSUによる交通流予測モデルへの刷新**:
   - `UpdatePredictiveRmrCbr` を廃止。
   - RSUクラス（またはグローバル関数）にて、上流流入量 $q_{in}$ と下流流出量 $q_{out}$ を用いた予測式 `CBR_est = currentCbr + ((qIn - qOut) * Δt * λ * S_avg) / R` を実装すること。

2. **オブジェクト単位のRスコア計算の実装**:
   - CPM内の各物標に対し、`R = N / (L * min(l, v))` (N:受信回数, L:距離, l:位置変化, v:速度変化) を算出。
   - スコアに基づいて各物標を `High` (低冗長/高重要) と `Low` (高冗長/低重要) に分類すること。

3. **三状態ハイブリッド制御の実行**:
   - `通常 (CBR_est < switchCbr)`: 全オブジェクトをV2Vで送信。
   - `警戒 (switchCbr <= CBR_est < maxCbr)`: 
     - `High` オブジェクト -> V2Vで送信。
     - `Low` オブジェクト -> 確率 $p$ でV2V、残りをV2N(MEC)へオフロード。
   - `逼迫 (CBR_est >= maxCbr)`: 
     - `High` オブジェクト -> **V2VとV2Nの二重送信 (Redundant Tx)**。
     - `Low` オブジェクト -> 最小確率 $p_{min}$ でV2Nへオフロード。

4. **不要ロジックのパージ**:
   - 実装非対象である `NR-V2X Sidelink` 関連の全てのHelper、Device、条件分岐を削除。
   - `DENM` 関連のコードを排除し、`CPM` の制御にリソースを集中させること。

5. **バックグラウンド干渉の維持**:
   - 山崎氏の最悪条件環境（1500B / 20ms周期 / ユニキャスト送信ノード）の設置ロジックを維持すること。

# Deliverables
- 修正された `V2VHybridCbr80211pNrv2x.cc` の完成コード。
- オブジェクトフィルタリングを可能にするための `BSContainer` および `CPBasicService` への介入ロジック。
```

---

### 次のステップへの案内
このプロンプトをCodexに投げた際、AIが「VaN3Twinの内部関数（`getLDM`など）が不明です」と返してくる場合があります。その場合は、最初にご提示いただいたソースコードの特定の箇所を再度読み取らせるか、私が修正版として提示したC++の断片コードを合わせて学習させてください。

# Role
ns-3 (VaN3Twin) シミュレーションエンジニア。V2X通信およびITSスタックの専門家。

# Goal
提供されたベースコードを修正し、修士論文の核となる「交通流予測に基づくV2V/V2N適応型ハイブリッド経路制御」を厳密に実装すること。

# Inputs (Context)
- `resume_0604.md`: 提案手法（通常・警戒・逼迫の3状態）の設計図。
- `研究内容詳細.md`: 評価環境（RSU予測式、干渉ノード、重要度分類）の仕様。
- `master_thesis_SYamasaki`: 山崎論文の $R$ スコア計算定義。

# Strict Implementation Constraints (絶対防衛線)
1. **RSUによる交通流予測モデルへの刷新**:
   - `UpdatePredictiveRmrCbr` を廃止。
   - RSUクラス（またはグローバル関数）にて、上流流入量 $q_{in}$ と下流流出量 $q_{out}$ を用いた予測式 `CBR_est = currentCbr + ((qIn - qOut) * Δt * λ * S_avg) / R` を実装すること。

2. **オブジェクト単位のRスコア計算の実装**:
   - CPM内の各物標に対し、`R = N / (L * min(l, v))` (N:受信回数, L:距離, l:位置変化, v:速度変化) を算出。
   - スコアに基づいて各物標を `High` (低冗長/高重要) と `Low` (高冗長/低重要) に分類すること。

3. **三状態ハイブリッド制御の実行**:
   - `通常 (CBR_est < switchCbr)`: 全オブジェクトをV2Vで送信。
   - `警戒 (switchCbr <= CBR_est < maxCbr)`: 
     - `High` オブジェクト -> V2Vで送信。
     - `Low` オブジェクト -> 確率 $p$ でV2V、残りをV2N(MEC)へオフロード。
   - `逼迫 (CBR_est >= maxCbr)`: 
     - `High` オブジェクト -> **V2VとV2Nの二重送信 (Redundant Tx)**。
     - `Low` オブジェクト -> 最小確率 $p_{min}$ でV2Nへオフロード。

4. **不要ロジックのパージ**:
   - 実装非対象である `NR-V2X Sidelink` 関連の全てのHelper、Device、条件分岐を削除。
   - `DENM` 関連のコードを排除し、`CPM` の制御にリソースを集中させること。

5. **バックグラウンド干渉の維持**:
   - 山崎氏の最悪条件環境（1500B / 20ms周期 / ユニキャスト送信ノード）の設置ロジックを維持すること。

# Deliverables
- 修正された `V2VHybridCbr80211pNrv2x.cc` の完成コード。
- オブジェクトフィルタリングを可能にするための `BSContainer` および `CPBasicService` への介入ロジック。